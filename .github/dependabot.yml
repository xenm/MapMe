# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  - package-ecosystem: "npm" # Look for `package.json` and `lock` files in the `root` directory
    directory: "/"
    schedule:
      interval: "daily"
  - package-ecosystem: "docker" # Look for a `Dockerfile` in the `root` directory
    directory: "/"
    schedule:
      interval: "daily"
  - package-ecosystem: "nuget"
    directory: "/nuget/helpers/lib/NuGetUpdater"
    schedule:
      interval: "daily"
  #
  # Ignored while .NET 10 is in Prerelease
  #
  # - package-ecosystem: "dotnet-sdk"
  #   directory: "/nuget/helpers/lib/NuGetUpdater"
  #   schedule:
  #     interval: "daily"
